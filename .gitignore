# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Local secrets (contains service-role key — never commit)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
~

# Phase 0 migration baseline: 16MB of rendered HTML snapshots.
# Regenerate with scripts/capture-baseline-html.sh. The extracted
# metadata.tsv IS committed — it is the Phase 8 diff target.
docs/baseline/html/
.next/
*.tsbuildinfo
